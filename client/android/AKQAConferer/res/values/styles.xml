<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowTitleSize">@dimen/title_size</item>
        <item name="android:windowTitleBackgroundStyle">@style/WindowTitleBackgroundStyle</item>
        <item name="android:windowTitleStyle">@style/WindowTitleStyle</item>
    </style>

    <style name="WindowTitleBackgroundStyle">
        <item name="android:background">@color/titlebar_color</item>
    </style>

    <style name="WindowTitleStyle" parent="android:TextAppearance.WindowTitle">
        <item name="android:textSize">@dimen/title_text_size</item>
        <item name="android:padding">@dimen/title_padding</item>
        <item name="android:textColor">@color/title_color</item>
        <item name="android:drawableLeft">@drawable/ic_launcher</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>